# oracle_loop/

Planning-only (no code yet). Design for a future investigation: use the full-data
"ceiling" GP as an **oracle cell** and the lucent image generator as an **acquisition
strategy** in a simulated (in-silico) closed loop, compared against the current
pool-based active/random selection.

**Start here: `HANDOFF.md`** — the full plan, the two-part split (validate oracle-as-cell
by reproducing the v0.7 explained-variance figure first, then add generation), the locked
decisions, the dataset/engine bridge + branch questions to resolve with the user, and the
ordered reading list.
