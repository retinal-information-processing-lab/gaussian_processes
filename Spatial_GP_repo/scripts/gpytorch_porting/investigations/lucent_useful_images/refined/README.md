# refined/ — committed final figures

Distilled, committable figures (PNG only — SVGs are not generated). The active deliverable currently lives
in `out/panels/` (per request). The early fixed-M=50 grid/hero figures are archived in
`../early_exploration_fixed_M50/`.
