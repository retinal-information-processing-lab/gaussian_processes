# docs/ — methods & theory notes (no images)

Write-ups for this investigation, e.g. `lucent_explainer.md` (plain-language recap of how
lucent's Fourier + sigmoid parameterization works). No figures here — see `out/`,
`refined/`, and `../early_exploration_fixed_M50/`.
