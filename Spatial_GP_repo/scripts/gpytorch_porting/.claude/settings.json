{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "PYTHON_PATH=$(which python) && if [[ \"$PYTHON_PATH\" != *pytorch_gpytorch* ]]; then echo \"CONDA_ENV_WRONG: Python is at $PYTHON_PATH, expected pytorch_gpytorch environment\"; exit 1; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
